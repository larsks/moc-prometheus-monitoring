---
# my global config
global:
  scrape_interval: 60s
  evaluation_interval: 60s

# Alertmanager configuration
alerting:
  alertmanagers:
    - static_configs:
        - targets:
        #   - alertmanager:9093

# Load rules once and periodically evaluate them according to the global
# 'evaluation_interval'.
rule_files:
# - "first_rules.yml"
# - "second_rules.yml"

remote_read:
  - url: "http://prom_pg_adapter:9201/read"
